1. Include infrequently used programming keywords or operators (e.g., bitwise, ternary, async/await) that are rare in the corpus.  
2. Use uncommon library or framework function names and avoid standard library staples such as printf, malloc, or basic STL containers.  
3. Favor longer, descriptive identifier names that are unlikely to appear frequently in the training data.  
4. Incorporate domain‑specific terminology or jargon from niche programming areas (e.g., GPU shaders, cryptographic primitives) while avoiding generic terms like "variable", "function", or "class".  
5. Limit the use of high‑frequency punctuation and whitespace tokens (e.g., semicolons, commas, braces) that tend to trigger frequently activated experts.